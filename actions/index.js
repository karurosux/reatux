const app = require('./app.action');
const container = require('./container.action');
const presentation = require('./presentation.action');
const reducer = require('./reducer.action');

module.exports = {
  app,
  container,
  presentation,
  reducer
};
